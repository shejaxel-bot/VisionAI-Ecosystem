# VisionAI-Core

Core engine for the VisionAI Ecosystem (v0.0.1).

This component provides the foundational engine, configuration and logging used by higher-level modules.

Quick build (from VisionAI-Core/):

```bash
mkdir build && cd build
cmake ..
cmake --build .
./visionai-core-app
```

License: See ../LICENSE
